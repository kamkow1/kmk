set GLOBAL = "123";

function testFunction(a, b) return ret
begin
	set ret = "text abjksdhgjkhdfjk";
end

function main(a1, a2)
begin
	set globalAccess = $GLOBAL;
	print($globalAccess);

	set variable = "some text";
	print("hello", "world", $a1);
	print("ahhaahhah", $variable);

	set variable = "some text 2222222";
	print("ahhaahhah", $variable);
	unset variable;
	# print("ahhaahhah ", $variable); # this should fail
	set condition = true;
	print($condition);
	
	set condition = false;
	print($condition);

	set condition = true;
	when ($condition)
	begin
		print("inside of when block", $condition);
	end;

	set r = testFunction($a1, $a2);
	print("r =", $r);
end

main("a", "b");
